apiVersion: v1
kind: Service
metadata:
  name: mys
spec:
  type: NodePort
  selector:
    app: run  # Changed 'apps' to 'app'
  ports:
    - port: 3000
      targetPort: 3030
      nodePort: 3000  # Optional: NodePort range is 30000-32767
